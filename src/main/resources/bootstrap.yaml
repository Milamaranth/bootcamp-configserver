
server:
    port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pruedaesntt/bootCamp-ConfigServer
          search-paths:
          - '*.yaml'
          default-label: master
